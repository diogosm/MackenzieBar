# Execução

para executar:

```sh
python servery.py
```

# Dependências

Este código roda no python3, outras dependências podem ser conhecidas no requirements e nos imports do código. A execução foi feita em um env do conda.

Para acesso de outro dispositivo, é necessário abrir as portas do firewall, principalmente se estiver utilizando antivírus.
